<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yofun.mapper.ProductMapper">

    <insert id="insertProduct" parameterType="com.yofun.model.Product">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into product(producttypeid, producttitle, productprice, merchantid, createdat, auditstatus, stocknumber, sellnumber, productimageurl) value(#{productTypeId}, #{productTitle}, #{productPrice}, #{merchantId}, #{createdAt}, #{auditStatus}, #{stockNumber}, #{sellNumber}, #{productImageUrl})
    </insert>

</mapper>